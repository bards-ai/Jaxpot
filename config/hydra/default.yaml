run:
  dir: outputs/${now:%Y-%m-%d}/${hydra:job.config_name}/${experiment_name}_${now:%H-%M-%S}
sweep:
  dir: outputs/${now:%Y-%m-%d}/${hydra:job.config_name}/${experiment_name}_${now:%H-%M-%S}
  subdir: ${hydra.job.num}

job_logging:
  handlers:
    file:
      filename: ${hydra.runtime.output_dir}/${hydra.job.name}.log
