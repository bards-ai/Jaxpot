# @package _global_

defaults:
  - override /logger: multi
  - override /model: resnet
  - override /trainer: alphazero
  - override /loguru: default
  - override /env: go/9x9
  - override /experiment: null
  - override /train_agent: alphazero
  - override /eval: go/pgx
  - override /advantage_gatherer: per_seat_return
  - _self_

trainer:
  num_epochs: 3
  batch_size: 8192
  max_grad_norm: 1.0
  value_coeff: 1.0
  policy_coeff: 1.0

train_agent:
  mcts_config:
    num_simulations: 32
    max_num_considered_actions: 16 # all Go 9x9 actions (81 intersections + pass)
    gamma: 1.0

seed: 42
lr: 0.001
lr_schedule: "constant"
lr_end: 1e-5
lr_warmup_iters: 0
multi_gpu: false
selfplay_num_envs: 1024
random_num_envs: 0
league_num_envs: 0
archive_num_envs: 0
num_eval_envs: 256
num_steps: 256
total_iters: 400
grad_accum_steps: 1

save_every: 5
keep_last_k: 10
best_checkpoint_top_k: 10
resume_from: null
env_history_len: 1

# League configuration
league_max_size: 8
league_add_every: 0
league_add_every_startup: 0
league_temp: 1.0
league_score_alpha: 0.1
warmup_iters: 400
league_log_every: 100

# Archive configuration
archive_max_size: 32
forgotten_eval_every: 500
forgotten_result_margin: 0.001
forgotten_min_episodes: 50_000

tags: ["go_9x9", "resnet", "alphazero"]
experiment_name: "Go9x9_resnet_alphazero"

base_unit: 256
use_target_selfplay: false
dump_debug_every: null
