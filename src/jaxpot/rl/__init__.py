"""Reinforcement learning trainers and losses.

Submodules (`trainer`, `ppo_trainer`, `alphazero_trainer`, `losses`, `utils`)
are imported lazily by their fully-qualified path to avoid circular imports
between `jaxpot.rl` and `jaxpot.models` / `jaxpot.env`.
"""
